package com.jackting.core.util;

import android.widget.ImageView;


public class ImageUtil {
    /**
     * 加载相对路径的图片
     * @param imageView
     * @param path
     */
    public static void load(ImageView imageView, String path){
//        String wholePath= CoreConstant.BASE_URL+path;
//        Glide.with(AndroidApplication.getContext()).load(wholePath)
//                .into(imageView);
    }
}
